# Local development overrides
# Values here supplement the primary .env file and enable an offline-friendly setup.

# Override DATABASE_URL for local smoke tests to avoid remote Postgres dependency
DATABASE_URL=sqlite+pysqlite:///northstar.db
# Router stays static for local smoke unless experimenting with learned mode
ROUTER_MODE=static
POLICY_ENFORCEMENT=strict
POLICY_DENY_PROJECTS=
POLICY_PROOF_MODE=false
ISOLATION_REPORT_DIR=./artifacts/isolation
EVAL_DATA_DIR=./eval/data
EVAL_MODE=false
ABMAP_ENABLED=false
# Uncomment below if you need to run against a local Postgres instance.
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=northstar
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_SSLMODE=disable
# POSTGRES_CONNECT_TIMEOUT=5

# Avoid Redis dependency during local smoke tests
QUEUE_MODE=direct
REDIS_URL=

# LLM provider inherits from primary .env (Cerebras). Uncomment below to force heuristic fallback.
# LLM_PROVIDER=openai
# OPENAI_API_KEY=
