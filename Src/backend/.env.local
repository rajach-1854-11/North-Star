# Local development overrides
# Values here supplement the primary .env file and enable an offline-friendly setup.

# Override DATABASE_URL for local smoke tests to avoid remote Postgres dependency
DATABASE_URL=sqlite+pysqlite:///northstar.db
# Uncomment below if you need to run against a local Postgres instance.
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=northstar
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_SSLMODE=disable
# POSTGRES_CONNECT_TIMEOUT=5

# Avoid Redis dependency during local smoke tests
QUEUE_MODE=direct
REDIS_URL=

# Use heuristic planner fallback when OpenAI credentials are not configured
LLM_PROVIDER=openai
OPENAI_API_KEY=
